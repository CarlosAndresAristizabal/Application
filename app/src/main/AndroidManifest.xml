<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mascotasapplication">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MascotasApplication">
        <activity android:name=".VIsta.Principales.Detalle_Mascota"
            android:parentActivityName=".VIsta.Principales.InicioPrincipalMascotas" /> <!-- Soporte para versiones 4.0 o menos -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".VIsta.Principales.InicioPrincipalMascotas" />
        <activity
            android:name=".VIsta.Menu.Acercade"
            android:parentActivityName=".VIsta.Principales.InicioPrincipalMascotas" /> <!-- Soporte para versiones 4.0 o menos -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".VIsta.Principales.InicioPrincipalMascotas" />

        <activity
            android:name=".VIsta.Menu.Contacto"
            android:parentActivityName=".VIsta.Principales.InicioPrincipalMascotas" /> <!-- Soporte para versiones 4.0 o menos -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".VIsta.Principales.InicioPrincipalMascotas" /> <!-- Soporte para versiones 4.0 o menos -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".VIsta.Principales.InicioPrincipalMascotas" />

        <activity
            android:name=".VIsta.Menu.Favorito"
            android:parentActivityName=".VIsta.Principales.InicioPrincipalMascotas" /> <!-- Soporte para versiones 4.0 o menos -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".VIsta.Principales.InicioPrincipalMascotas" />

        <activity android:name=".VIsta.Principales.InicioPrincipalMascotas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>